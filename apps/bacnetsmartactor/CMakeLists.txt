cmake_minimum_required(VERSION 3.5)
ENABLE_LANGUAGE(C CXX)
include (TestBigEndian)

set(EXTRA_COMPONENT_DIRS "../../components")
set(COMPONENTS main bootloader esptool_py freertos driver lwip cxx bacnet-stack nvs_flash)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
TEST_BIG_ENDIAN(IS_BIG_ENDIAN)
project(bacnetsmartactor)
if(IS_BIG_ENDIAN)
    target_compile_definitions(bacnet-stack PUBLIC BIG_ENDIAN BACDL_BIP)
else()
	target_compile_definitions(bacnet-stack PUBLIC BACDL_BIP)
endif()
